// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  loginUrl:'https://nodejs-acli.onrender.com/datas/login',
  registerUrl:'https://nodejs-acli.onrender.com/datas/register',
  displayUrl:'https://nodejs-acli.onrender.com/datas',
  upUrl:'https://nodejs-acli.onrender.com/datas'

  // loginUrl:'http://localhost:3000/datas/login',
  // registerUrl:'http://localhost:3000/datas/register',
  // displayUrl:'http://localhost:3000/datas',
  // upUrl:'http://localhost:3000/datas'

};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
